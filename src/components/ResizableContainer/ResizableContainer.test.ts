import { mount } from '@vue/test-utils'
import ResizableContainer from './ResizableContainer.vue'

describe('Test <ResizableContainer />', () => {
  test('it should render successfully', () => {
    expect(ResizableContainer).toBeTruthy()
  })
})
