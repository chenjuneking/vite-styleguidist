export const RESIZABLE_CONTAINER_PERSISTENT_KEY_PREFIX = '$$_rcp__'

export enum EStorageType {
  SESSION_STORAGE,
  LOCAL_STORAGE,
}

export enum ENetworkStatus {
  ONLINE = 'online',
  OFFLINE = 'offline',
}

export enum ETheme {
  DARK = 'dark',
  LIGHT = 'light',
}

export enum EDevice {
  MOBILE = 'mobile',
  TABLET = 'tablet',
  DESKTOP = 'desktop',
}

export { SQL_KEYWORDS } from './sqlKeywords'
